# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [未发布]

- macOS 搜索页中的排行榜筛选改用原生下拉选择器，去除七项分段控件撑大窗口的固有宽度；列表标题允许在现有列宽内换行。
- 信息流左滑菜单的“稍后再看”在原位置切换为“移除”，不再切换到另一侧；复用同一个操作组件，保存期间禁用重复点击。

- 修复 macOS 原生菜单回调转交配置时的 Sendable 转换警告，显式声明主线程可发送闭包。
- 搜索历史面板恢复单层连续圆角 Liquid Glass，保留独立文字层与按压弹性。
- 列表与瀑布流增加跨平台“加载上一页”入口；向前插页保存可见项目及视口内偏移，等待实际布局更新后恢复，替代网络任务中的延迟 ID 赋值。取消加载也会释放忙碌状态。

- 修复网页登录导航事件 switch 对未来未知枚举值缺少兜底所导致的 Swift 6 编译错误。
- 搜索栏文字与玻璃渲染层彻底分离，移除共享玻璃合成，避免文字被折射或缩放而模糊；输入状态下触摸搜索栏、历史面板增加背景弹簧反馈。被动触摸观察保留原生光标、选词、按钮与滚动手势，滚动、取消、后台时复位，遵守减弱动态效果。
- 搜索按压状态封装在独立背景组件中，避免触摸反馈触发整个搜索页面及历史列表更新。

- 搜索栏加入标准放大镜与键盘搜索提交，统一高度、保留玻璃形变；历史/已保存搜索面板改为单层系统材质列表，按内容及可用高度收缩，减少空白，键盘候选自动滚入可见区域。历史匹配支持部分关键词、大小写和本地化匹配，并可查找最近五条之外的历史。
- 已保存搜索读写移至串行后台 actor，面板模型跨展开保留；并发保存去重、旧读取结果失效，失败的写入不自动重试，避免主线程数据库工作挤占展开动画。
- 补充首次预览菜单/预览生命周期与启动预热的性能埋点，以及仅 Debug 生效的分享/自定义预览/滚动标题/预热对照参数；采样说明见 PREVIEW_PROFILING.md，尚未判定真机首次卡顿根因。

- 修正非网站排序收藏封面被裁成统一比例的问题：收藏索引新增宽高字段并保留原始比例，旧索引按已解码图片尺寸修正；静态/动态封面及长按预览均完整适配图片，不再以填充裁切掩盖比例缺失。
- 搜索栏统一使用 `GlassEffectContainer` 和稳定玻璃标识，聚焦时以原生弹簧展开；关闭/视图切换按钮保持同一控件身份、图标使用系统替换过渡，跳页按钮与玻璃表面协调形变。交互玻璃背景与文字输入层分离，保留选词和光标手势，遵守减弱动态效果。
- 详情紧凑头部改用直接附着在内容上的单层连续圆角 Liquid Glass，保留半透明镜片与封面光晕；移除延伸到安全区的零圆角玻璃矩形，以系统滚动边缘效果衔接顶部，减少直角折射。

- 恢复瀑布流长按预览的长标题往返滚动：使用 SwiftUI 原生上下文菜单的实时预览，短标题不滚动、两端停留，遵守减弱动态效果；由系统管理原卡片的呈现与归还。
- 修复收藏瀑布流排序后的封面重叠：布局结果与重建版本整体发布，排序/替换/列宽变化时重建懒加载测量缓存，追加分页保留原缓存；封面改用由元数据确定比例的容器，避免图片加载改变布局高度。后台分列任务支持取消。
- macOS 信息流菜单按操作状态和语言缓存，普通视图更新仅刷新动作数据，减少重复构建菜单与符号；新增排序测量缓存和标题滚动时序回归测试。

- iOS/iPadOS 信息流长按改用 SwiftUI 原生 `contextMenu`，由系统直接抬起并归还原卡片；移除透明 UIKit 交互层、自制预览、手动隐藏/恢复与菜单预热。分享继续使用真实 URL 和本地标题；首次系统初始化耗时仍需真机验证。
- 瀑布流每列改为 `LazyVStack`，底部分页改为可见性触发；布局缓存比较完整有序数据，覆盖内部排序、尺寸与元数据更新，新增排序缓存和追加分页稳定性回归测试。
- 扩大移动端详情快捷按钮的点击区域并补充读屏标签；瀑布流标题高度随字体缩放、悬浮滚动遵守减弱动态效果；滚动视图中的动图仅在可见且应用活跃时播放，离屏、后台与视图移除时暂停。

- 修复长按预览结束后原元素可能不恢复：隐藏改为保持 SwiftUI 视图结构的透明度更新；恢复回调独立于菜单协调器生命周期，视图移除时清理，并防止旧退场回调覆盖新菜单状态。首次系统菜单/分享卡顿暂时搁置，保留耗时诊断。

- 优化 iOS/iPadOS 首次长按与画廊分享路径：移除启动时加载占位分享控制器，分享使用真实 URL 与本地标题预览；菜单分享等待退场完成后呈现，避免动画和系统初始化重叠。长按预览使用系统语义背景，减少额外玻璃渲染开销，标题滚动遵守“减弱动态效果”；新增分享控制器构建耗时标记。冷启动改善幅度仍需真机测量。

- 缩短首次画廊载入的关键路径：详情解析器在后台预热，详情到达后立即呈现，并行恢复下载、收藏与评论状态；以传统角向渐变替换首屏三重 MeshGradient 与嵌套离屏合成，避免首次图形管线编译卡顿。
- 信息流封面操作彻底脱离 SwiftUI 手势和行内弹窗状态：macOS 在显示单元格时预构建原生 NSMenu，iOS 使用就地弹出的原生 UIContextMenu；列表预览会放大完整横向项目，瀑布流预览则放大封面并滚动长标题。菜单符号与分享控制器在首帧后的空闲阶段准备，第一次分享直接复用准备结果，避免在长按或菜单退场动画中同步初始化系统服务。
- 修正瀑布流长按预览在完成布局前计算标题宽度、导致跑马灯不启动的问题；macOS 列表与瀑布流均可直接右键执行稍后再看、下载、收藏、复制链接和分享，评论发表按钮统一为胶囊轮廓。
- 长按菜单直接复用信息流已经解码并显示的封面，菜单呈现期间隐藏原项目以避免与放大预览重叠；预览背景改用系统 Liquid Glass，瀑布流长标题以更快速度从左对齐位置开始滚动。诊断日志导出改用跨平台 SwiftUI `fileExporter`，移除 UIKit 分享包装和 macOS 阻塞式保存面板。
- 修复上下文菜单放大预览期间底层项目提前显现的问题：底层内容保持隐藏至系统收回动画完成，并移除列表预览底部的手工材质裁切伪影；移动端发表评论改为同高圆形图标，macOS 保持同高胶囊。网页登录迁移到系统 26 SwiftUI `WebView`/`WebPage`，合并两套平台桥接并保留前进后退手势与 Cookie 登录检测。
- 统一阅读器顶部页码胶囊与关闭、设置按钮的高度；评论输入框改为长胶囊，并对齐关闭和发表控件的触控高度。
- 评论撰写区改为单层整体 Liquid Glass 胶囊，文本框保持透明；评论长按菜单提供复制、分享与系统翻译。
- macOS 主窗口继续禁用多窗口场景恢复，但单独保存并恢复上次窗口大小和位置。
- 优化 iOS/iPadOS 冷启动：磁盘 URL 缓存与持久搜索结果移出主线程，凭据和请求栈并行恢复，并错峰启动下载、快捷指令及 Spotlight 服务。
- 以图搜图入口移至搜索框右端；预览图长按或右键菜单可载入原图进行搜图，仍需用户确认后才上传。
- 搜索历史面板改用原生弹簧过渡与滚动回弹，聚焦时的展开和触摸反馈更加连贯。
- 修复 IP 限制模式被误报为配额读取失败：正确显示“目前没有限制”，不虚构数字额度或将临时额度开通费用当作重置费用；区分登录失效、网页验证和未知格式。
- 新增原生以图搜图：照片/文件选择、上传前预览、相似/精确匹配及仅封面选项；主动确认后上传，结果进入持久搜索页，复用连续分页及搜索返回历史。
- 图片准备在后台限尺寸解码，相似搜索去除元数据，精确匹配保留原文件；校验结果跳转域名，隔离不同图片搜索缓存，补齐三语文案。
- 预览箭头以最新目标页累计操作，使用原生页首滚动定位，隔离过期滚动回调；相邻页跳转保留已加载内容，用户滚动可打断定位。
- 设置账号区域新增只读图片配额面板：已用、剩余、上限、网站重置费用与更新时间；支持手动刷新，失败保留带时间的旧数据，不在应用内扣费重置。
- 配额解析兼容网页空白、千位分隔符及缺失费用；登录页或异常内容不再误显示为零配额，并补齐三语文案。
- 修复预览跳页将“下一页”误作总页数的问题，统一上下分页栏解析并保留已知总数；预览请求改为只解析一次 HTML。
- 预览页码胶囊移除冗余文字并降低高度，滑块跟随应用主题色；扩大 macOS 预览窗口并按可用桌面高度限制初始尺寸。
- 画廊更新检查支持中断后继续及仅重试失败项，保留已完成结果与请求节流；补充同步页数、剩余数量和空状态，隔离账号/站点切换后的旧响应。
- macOS 系统“关于”补充作者、上游作者与可点击的项目链接。
- 新增收藏与下载的手动新版检查：分批查询、可取消、区分检查失败，不自动下载或替换旧版；收藏索引未完整同步时不再清理旧索引。
- “关于”补充分支作者 kGMia、上游作者 felixchaos 与项目链接；关于和协议使用独立原生弹窗，避免占用分栏的画廊详情。
- 全部预览增加 Liquid Glass 页码胶囊、滑块跳页及双向连续加载，并补齐三语文案。
- 启动和登录时的钥匙串操作改为后台串行执行，首页等待凭据恢复；合并重复的登录后请求，保留冷启动收到的画廊链接。
- 修复同名 Cookie 导致的崩溃风险；封面视图重建时立即复用已解码缓存，保留原生长按菜单样式。
- 修复阅读器加载原图时误用普通图片缓存的问题，本地下载优先于在线阅读缓存。
- 合并同一页的后台读取和解码任务，阻止取消、重试或切换画廊前的旧请求写回当前状态。
- 阅读缓存容量调整即时生效；缓存统计和清理移至后台，避免阻塞设置界面。
- 减少逐张缓存图片时的全目录扫描，使用原子写入，并修复长缓存键可能混淆页码的问题。

## [1.0.0] - 2026-09-02

### 主要更新

- 完成 macOS、iOS 与 iPadOS 的 SwiftUI 原生化和响应式布局改造。
- 重构首页、搜索、收藏、下载、历史、“我的”和画廊详情导航。
- 完善瀑布流、连续分页、多层搜索、稍后再看与本地收藏能力。
- 重构阅读器翻页、单双页、手势、全屏、翻译和主题背景。
- 增加下载 Live Activity、App Shortcuts、原生菜单和系统接力支持。
- 完成简体中文、繁体中文（台湾）与英文（美国）本地化基础支持。
- 加固缓存、网络、Cookie、数据库和图片解码的并发与性能表现。
- 增加隐私清单、发行预检和双平台 Release 静态分析流程。

### 已知限制

- 收藏瀑布流在切换全局排序后仍可能出现布局异常，建议发行版暂时使用列表视图排序。
- 部分设备或系统环境首次打开原生上下文菜单、分享面板时仍可能出现一次性的系统服务初始化停顿；本轮暂不继续以替换交互样式为代价规避。
- 受目标站点内容属性影响，公开分发前仍需独立评估商店内容审核要求。

## [0.1.0] - 2026-02-15

### 🎉 首次发布

#### 新功能
- **画廊浏览** — 支持 E-Hentai / ExHentai 画廊列表、热门、最新
- **排行榜** — TopList 排行榜浏览
- **高级搜索** — 分类筛选、关键词、标签搜索
- **快速搜索** — 搜索条件收藏与快速调用
- **收藏管理** — 多文件夹云端收藏同步
- **浏览历史** — 本地浏览记录管理
- **画廊详情** — 标签、评论、预览图、元数据展示
- **图片阅读器** — 横向翻页 / 纵向滚动双模式
  - 缩放手势支持（水平/纵向模式）
  - 滑动返回手势
  - HUD 叠加层（电量、时间、进度）
- **下载管理** — 后台下载、断点续传、进度通知
- **多种登录方式** — 账号密码、网页登录、Cookie 导入、跳过登录
- **安全保护** — Face ID / Touch ID / 密码锁
- **站点切换** — E-Hentai / ExHentai 一键切换
- **设置中心** — 阅读器偏好、网络配置、数据管理

#### 网络
- Domain Fronting 回退机制
- DNS over HTTPS 支持
- Cloudflare 403 检测与友好提示
- WebView 原生 UA 登录（兼容 Cloudflare Turnstile）

#### 平台支持
- iOS 17+ / iPadOS 17+
- macOS 14+ (Sonoma)

#### 架构
- Swift Package Manager 模块化架构（EhCore、EhNetwork、EhParser、EhSpider、EhDownload、EhUI）
- Swift 6 严格并发安全
- SwiftUI 原生构建
- Network retries are now bounded to idempotent reads, honor cancellation and
  `Retry-After`, while comments, votes, and favorites are never submitted twice.
- Network diagnostics no longer performs blocking DNS work on the main thread
  and now reports the selected site, login-cookie state, HTTP status, and latency.
- API documents, thumbnails, and reader images use separate cache budgets; the
  Settings cache panel reports and clears each category independently.
- Remember the ExHentai switch decision per account so iOS does not ask again
  on every launch after the user has already chosen.
